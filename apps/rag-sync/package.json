{
  "name": "@docmost/rag-sync",
  "version": "0.34.1",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "corepack pnpm run build && node --test \"dist/**/*.spec.js\"",
    "start": "node dist/main.js"
  },
  "dependencies": {
    "@docmost/api-contract": "workspace:*",
    "ioredis": "^5.4.1"
  },
  "devDependencies": {
    "@types/node": "^22.13.4",
    "typescript": "^5.7.3"
  }
}
