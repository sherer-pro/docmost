.env
.env.*
!.env.example
!.env.compose.example
# Keep obsolete standalone-sync files local while operators remove them.
rag-sync.config.json
/.rag-sync
**/DOCMOST_RAG_KEY
**/OPEN_WEBUI_KEY
secrets/
data
# Local audit, e2e and scratch artifacts. These runs produce gigabytes of
# Playwright reports, traces and downloads. Curated evidence that must live in
# the repository is added deliberately with `git add -f`.
/output/
/tmp/
# compiled output
/dist
/node_modules
# Build output of the retired standalone rag-sync worker.
/apps/rag-sync/dist/
/apps/rag-sync/node_modules/

# Logs
logs
*.log
npm-debug.log*
pnpm-debug.log*
yarn-debug.log*
yarn-error.log*
lerna-debug.log*

# OS
.DS_Store

# Tests
/coverage
/.nyc_output

# IDEs and editors
/.idea
.project
.classpath
.c9/
*.launch
.settings/
*.sublime-workspace

# IDE - VSCode
.vscode/*
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/launch.json
!.vscode/extensions.json

.nx
.nx/installation
.nx/cache

# Local tool state and agent worktrees
/.graphify/
/.graphify-cache/
/.claude/worktrees/
/.codex/worktrees/
/.agents/worktrees/
/.worktrees/
/.junie/

# Graphify
graphify-out/*
!graphify-out/graph.json
!graphify-out/GRAPH_REPORT.md
!graphify-out/graph.html
!graphify-out/.graphify_analysis.json
!graphify-out/manifest.json
!graphify-out/.graphify_labels.json
.claude/settings.local.json

# TypeScript incremental build metadata
*.tsbuildinfo

# Windows
Thumbs.db
Desktop.ini
